#!/bin/sh

process() {
    while read input; do
        case "$input" in
            BLANK*|LOCK*) awesome-client 'pomodoro_pause() unfocus()' ;;
            UNBLANK*) awesome-client 'pomodoro_start() focus()' ;;
        esac
    done
}

xscreensaver-command -watch | process
